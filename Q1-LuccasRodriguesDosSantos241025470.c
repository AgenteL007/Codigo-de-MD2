#include <stdio.h>

int main ()
{
    printf ("Codificador e Decodificador de Luccas Rodrigues dos Santos (241025470)\n");

    int i;

    printf ("Digite o valor de p (ou mande um EOF para encerrar): ");

    while (scanf ("%d", &i) != EOF)
    {
        printf ("Digite o valor de q: ");
        int q;
        scanf ("%d", &q);

        printf ("Digite o valor de z: ");
        int z;
        scanf ("%d", &z);

    }

    return 0;
}